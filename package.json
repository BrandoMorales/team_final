{
  "name": "team-todo-api",
  "version": "1.0.0",
  "description": "API para la aplicación Team To-Do",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  }
}